import { Layout } from '~/components/molecules/layout';

export const Messages = () => {
  return (
    <Layout margin={[25]}>
      <Layout>here message first</Layout>
    </Layout>
  );
};
export default Messages;

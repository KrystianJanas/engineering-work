export const ManagementAnnouncements = ({
  changeNumber,
}: {
  changeNumber: () => void;
}) => {
  // TODO: onClick={changeNumber} should be used in announcement layout
  return <div>here zarządzanie</div>;
};
